import { Component } from '@angular/core';

@Component({
    standalone: true,
    selector: 'app-login',
    template: `
    <div>This is the login page    </div>
  `,
})
export class LoginComponent { }
